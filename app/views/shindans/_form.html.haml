= form_for @shindan do |f|
  - if @shindan.errors.any?
    #error_explanation
      %h2= "#{pluralize(@shindan.errors.count, "error")} prohibited this shindan from being saved:"
      %ul
        - @shindan.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
